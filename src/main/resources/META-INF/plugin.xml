<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>io.github.eskibear.copilot-cli-jetbrains</id>
    <name>GitHub Copilot CLI Launcher</name>
    <vendor url="https://github.com/Eskibear">Eskibear</vendor>
    <description><![CDATA[
        <p>Launches the <a href="https://github.com/github/copilot-cli">GitHub Copilot CLI</a>
        (<code>copilot</code>) in the IDE's integrated terminal.</p>
        <p>Adds two entry points:</p>
        <ul>
          <li><b>Tools | Launch GitHub Copilot CLI</b> menu action.</li>
          <li>A Copilot icon on the bottom-right status bar (single-click to launch).</li>
        </ul>
        <p>If the CLI is not installed, the plugin opens a terminal and runs the official install
        command for your platform (<code>winget install GitHub.Copilot</code> on Windows,
        <code>curl -fsSL https://gh.io/copilot-install | bash</code> on macOS / Linux).</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>org.jetbrains.plugins.terminal</depends>

    <extensions defaultExtensionNs="com.intellij">
        <statusBarWidgetFactory id="io.github.eskibear.copilotcli.StatusBarWidget"
                                implementation="io.github.eskibear.copilotcli.CopilotCliStatusBarWidgetFactory"
                                order="last"/>
        <postStartupActivity implementation="io.github.eskibear.copilotcli.bridge.IdeBridgeStartupActivity"/>
        <notificationGroup id="Copilot CLI Bridge" displayType="BALLOON"/>
    </extensions>

    <actions>
        <action id="CopilotCli.Launch"
                class="io.github.eskibear.copilotcli.LaunchCopilotCliAction"
                text="Launch GitHub Copilot CLI"
                description="Open the GitHub Copilot CLI in the integrated terminal. Installs it if missing.">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
        <action id="CopilotCli.RenameTab"
                class="io.github.eskibear.copilotcli.RenameCopilotCliTabAction"
                text="Rename..."
                description="Rename this terminal tab.">
            <add-to-group group-id="EditorTabPopupMenu" anchor="first"/>
        </action>
    </actions>
</idea-plugin>
